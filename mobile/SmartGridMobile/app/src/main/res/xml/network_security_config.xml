<?xml version="1.0" encoding="utf-8"?>
<!--
    Allows plain HTTP to the development machine hosting the SmartGrid Web API.
    10.0.2.2  -> host loopback as seen from the Android emulator
    localhost -> used when the API is port-forwarded onto the device with adb reverse
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>
